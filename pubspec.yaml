name: flutter_news_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.6.1 <4.0.0"  # Only include the Dart/Flutter SDK version constraints here

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6           # For API calls
  flutter_bloc: ^8.1.2
  equatable: ^2.0.5
  provider: ^6.0.5
  cached_network_image: ^3.2.3
  google_fonts: ^4.0.3
  shared_preferences: ^2.0.13
  json_annotation: ^4.8.0
  url_launcher: ^6.1.11
  cupertino_icons: ^1.0.8
  lottie: ^2.2.0
  shimmer: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/arts.png
  - assets/tech.png
  - assets/spots.png
  - assets/health.png
  - assets/fun.png
  - assets/science.png
  - assets/general.png
  - assets/music.png
  - assets/placeholder.png